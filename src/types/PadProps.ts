/**
 * Props for a Pad component.
 */
interface PadProps {
	/**
	 * The amount of buttons horizontally.
	 * @default 4
	 */
	columns: number;
	/**
 	 * The amount of buttons vertically.
 	 * @default 4
 	 */
	rows: number;
}

export default PadProps;